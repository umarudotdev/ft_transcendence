{
  "name": "@ft/game",
  "version": "1.0.0",
  "module": "src/app.config.ts",
  "scripts": {
    "dev": "bun run --watch src/app.config.ts",
    "build": "bun build src/app.config.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@colyseus/bun-websockets": "^0.17.0",
    "@colyseus/monitor": "^0.17.0",
    "@colyseus/schema": "^3.0.0",
    "@ft/logger": "workspace:*",
    "colyseus": "^0.17.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
